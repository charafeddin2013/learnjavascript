//We donot need to declare the variable type

//declare number variables
var number=10;
document.write("my number is"+number);

//declare booleans
var bool=true;
document.write("i am a boolean variable and my value is " + bool);

// create strings
var greetings = "Hello world";

//string length
var len=greetings.length;
document.write("The length of \""+ greetings +"\" is" +len);

// get the type of variables
document.write(typeof number + " " +typeof bool + " " +typeof greetings);
